// BMI Calculator:

// creating a variable of weight in KG:
let weightInKg = 66; 

// Creating a variable of height in meters:
let heightInMeters= 1.60; 

// Creatin a variable of BMI:
let BMI = weightInKg/( heightInMeters *heightInMeters);

// Printing BMI:
console.log(`" Your BMI is ${BMI}`);